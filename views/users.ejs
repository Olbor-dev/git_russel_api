<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
</head>
<body>
  <h1>Gestion des utilisateurs</h1>
  <a href="/dashboard">⬅ Retour au dashboard</a> |
  <a href="/logout">🚪 Déconnexion</a>

  <hr>

  <h2>Créer un nouvel utilisateur</h2>
  <form action="/users/create" method="POST">
    <label>Nom :</label>
    <input type="text" name="name" required>

    <label>Email :</label>
    <input type="email" name="email" required>

    <label>Mot de passe :</label>
    <input type="password" name="password" required>

    <button type="submit">Créer</button>
  </form>

  <hr>

  <h2>Liste des utilisateurs</h2>

  <% if (users.length === 0) { %>
    <p>Aucun utilisateur trouvé.</p>
  <% } else { %>
    <table border="1" cellspacing="0" cellpadding="5">
      <thead>
        <tr>
          <th>Nom</th>
          <th>Email 🔑</th>
          <th>Mots de passe</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach(user => { %>
          <tr>
            <form action="/users/update/<%= user._id %>" method="POST">
              <td><input type="text" name="name" value="<%= user.name %>" required></td>
              <td><input type="email" name="email" value="<%= user.email %>" required></td>
              <td>
              <!-- Champ mot de passe actuel -->
              <input type="password" name="currentPassword" placeholder="Actuel" required>
              <!-- Nouveau mot de passe (optionnel) -->
              <input type="password" name="newPassword" placeholder="Nouveau (laisser vide si inchangé)">
              </td>              
              <td>
                <button type="submit">💾 Sauvegarder</button>
                <a href="/users/delete/<%= user._id %>" onclick="return confirm('Supprimer cet utilisateur ?')">🗑 Supprimer</a>
              </td>
            </form>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</body>
</html>